import {ItemType} from "@/react/types/ItemType";
import {ProducerType} from "@/react/types/ProducerType";

export type IndexType = {
	ItemData    : ItemType[],
	ProducerData: ProducerType[],
}

export type ItemListType = {
	ItemData    : ItemType[],
}

export type ProducerLitType = {
	ProducerData: ProducerType[],
}
