import React from "react";

type WrapperType = {
    children: React.ReactNode
}

type ContainerType = {
    children: React.ReactNode,
}

type MainType = {
    children: React.ReactNode
}

export {
    WrapperType,
    ContainerType,
    MainType,
}
