import React from "react";

type ProducerGridType = {
	children: React.ReactNode
}

type _type = {
	title    : string,
	moreLink  : string,
	children  : React.ReactNode,
}

type SearchProducerListType = {
	children  : React.ReactNode,
}

export {
	ProducerGridType,
	_type,
	SearchProducerListType,
}
