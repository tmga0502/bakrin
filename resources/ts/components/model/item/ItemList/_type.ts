import React from "react";

type ItemGridType = {
	children: React.ReactNode
}

type _type = {
	title    : string,
	moreLink  : string,
	children  : React.ReactNode,
}

type SearchItemListType = {
	children  : React.ReactNode,
}

export {
	ItemGridType,
	_type,
	SearchItemListType,
}
